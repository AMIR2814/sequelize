status = {
    DISABLE: 'disable',
    ENABLE: 'enable',
}

roles = {
    ADMIN: 'admin',
    USER: 'user',
}

permission = {
    // admin
    GET_USERS: 'getUsers',
    MANAGE_USERS: 'manageUsers',

    // user
    
}

module.exports = {
    status,
    roles,
    permission,
}